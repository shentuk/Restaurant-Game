/*
 * 进度条类
 * 功能：用于显示菜品状态条和顾客等位条
 */

class ProgressBar {
    constructor(options) {
        const {
            container,
            progress,
            duration,
            callback
        } = options;
    }
}
export default ProgressBar;